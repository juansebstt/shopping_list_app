<!DOCTYPE html>
<html>
<head>
  <title>ShoppingListApp</title>

  <!-- Include CSRF meta tags for protecting against CSRF attacks -->
  <%= csrf_meta_tags %>

  <!-- Include Content Security Policy (CSP) meta tag for setting security policies -->
  <%= csp_meta_tag %>

  <!-- Include application stylesheet with Turbo Track for automatic page updates -->
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>

  <!-- Include application JavaScript with Turbo Track for automatic page updates -->
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>

<body>
  <!-- Include Turbo Tags for turbo-frame and turbo-stream elements -->
  <%= turbo_include_tags %>

  <!-- Render shared flash messages partial -->
  <%= render 'shared/flash_messages' %>

  <!-- Yield content to be rendered dynamically -->
  <%= yield %>
</body>
</html>
